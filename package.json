{
  "name": "repomap",
  "version": "0.1.0",
  "description": "AI-powered documentation generator for multi-repo projects",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/adapters/*"
  ],
  "scripts": {
    "build": "npm run build -w @repomap/core && npm run build -w @repomap/adapter-claude && npm run build -w @repomap/adapter-claude-code && npm run build -w @repomap/cli",
    "dev": "npm run dev -w packages/cli",
    "test": "jest"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  }
}
